FROM node:22-alpine AS build
WORKDIR /app
COPY client/package*.json ./
RUN npm install
COPY client ./
ARG VITE_CORE_URL=http://localhost:8787
ENV VITE_CORE_URL=$VITE_CORE_URL
RUN npm run build
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
